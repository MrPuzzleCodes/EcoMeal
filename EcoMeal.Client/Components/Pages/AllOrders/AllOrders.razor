@page "/orders/all"
@namespace EcoMeal.Client.Components.Pages.AllOrders
@using Microsoft.AspNetCore.Components.Authorization

<AuthorizeView Roles="Admin">
<Authorized>

<PageTitle>All Orders</PageTitle>
<div class="container">
    
    <div class="card my-4 rounded-4 border-success">
        <div class="card-body">
            <h1>All Orders</h1>
        </div>
    </div>

    @if(AllOrdersList is null){
        <p>Loading...</p>
    }else if(!AllOrdersList.Any()){
        <div class="alert">
            No Orders Exist.
        </div>
    }else{

        <div class="container">
            <div class="row g-3">
                @foreach(var order in AllOrdersList){
                    <div class="col-12 col-md-12 col-lg-6 col-xl-6 p-2">
                        <div class="card rounded-4 border-success">
                            <div class="card-body">
                                <div class="d-flex justify-content-between align-items-start">
                                    <h3 class = "card-title mb-1">@order.PackageName</h3>
                                    <p class="text-muted small">@order.Date.ToString("dd.MM.yyyy HH:mm")</p>
                                </div>

                                <div class="d-flex justify-content-between align-items-start">
                                    <p class="text-muted small">@order.BusinessName</p>
                                    @if(order.Status == 1){
                                        <span class="badge rounded=pill bg-success-subtle text-success-emphasis">Completed</span>
                                    }else{
                                        <span class="badge rounded=pill bg-danger-subtle text-danger-emphasis">Error</span>
                                    }
                                </div>
                                <div class="d-flex justify-content-between align-items-start">
                                    <div class="fw-semibold">@order.UserName</div>
                                    <div class="fw-semibold">@order.Price RON</div>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    }
</div>

</Authorized>

<NotAuthorized>
<RedirectToHome />
</NotAuthorized>

</AuthorizeView>